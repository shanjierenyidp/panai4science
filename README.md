# panai4science

Personal GitHub Pages website for Pan Du.

This is a static site: `index.html`, `styles.css`, and the image assets in `materials/` are enough for GitHub Pages. Open `index.html` directly in a browser for a local preview.

Before publishing, make sure the `materials/` folder is committed so the profile and research images load on GitHub Pages.
